# Loaded automatically by `vite build` (production mode).
# Overrides .env for production bundles.
# Secrets (Supabase keys, API keys) must be set as env vars in your host
# (Vercel / Railway / Fly.io) — do NOT add them here.

VITE_AUTH_ENABLED=true
